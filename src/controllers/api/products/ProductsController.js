import unimplements from '../../../class/unimplements.js';

class ProductsController {
  static runGetAll(req, res) {
    unimplements('runGetAll');
  }
  static get(req, res) {
    unimplements('get');
  }
}

export default ProductsController;
